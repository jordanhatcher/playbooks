---
all:
  hosts:
  children:
    homelab:
      hosts:
        sumo:
        warden:
    dns_servers:
      hosts:
        sumo:
        warden:
    vps:
      hosts:
        watchman:
    local:
      hosts:
        127.0.0.1:
          ansible_connection: local
